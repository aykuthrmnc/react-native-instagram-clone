import {View} from "react-native";

const Divider = () => {
  return (
    <View
      style={{
        height: 0.5,
        backgroundColor: "#DADADA",
      }}
    />
  );
};

export default Divider;
